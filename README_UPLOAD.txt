Missing files to upload to the GitHub repository root:

1. .github/workflows/tests.yml
   Enables GitHub Actions. After upload, GitHub will run pytest on every push.

2. notebooks/demo_temperature_prediction.ipynb
   A clean public demo notebook based only on synthetic data.

Upload the folders .github and notebooks from this extracted package into the repository root, not inside another nested folder.
